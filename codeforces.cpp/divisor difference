#include <iostream>
#include <algorithm>
#include <string>
#include <iomanip>
#include <cmath>
#include<vector>
using namespace std;
int main() {
    int t;
    cin >> t;

    vector<long long> numbers(t);

    for (int i = 0; i < t; i++) {
        cin >> numbers[i];
    }

    for (int i = 0; i < t; i++) {
        cout << numbers[i] - 1 << endl;
    }

    return 0;
    
}
